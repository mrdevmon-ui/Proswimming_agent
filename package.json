{
  "name": "my-max-bot",
  "version": "1.0.0",
  "description": "A simple bot for MAX messenger using Max Bot SDK",
  "main": "index.js",
  "dependencies": {
    "max-bot-sdk": "^latest",
    "dotenv": "^16.0.3" // Библиотека для работы с .env
  }
}
